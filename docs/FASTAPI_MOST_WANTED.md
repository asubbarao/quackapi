# FastAPI's Most-Wanted Features — quackapi Scoreboard

**Thesis.** quackapi's pitch is "the data layer IS the framework." The sharpest proof is that FastAPI's most-upvoted feature requests of all time — which FastAPI closed UNRESOLVED — are things quackapi either already ships or can ship as a few lines of SQL/DDL. This is the scoreboard.

| 👍 | Issue | FastAPI request | FastAPI outcome | quackapi status | How (the DDL / mechanism) |
|----|-------|-----------------|-----------------|-----------------|---------------------------|
| 83 | [#214](https://github.com/fastapi/fastapi/issues/214) | Bridge Pydantic ↔ SQLAlchemy models | closed unresolved | DISSOLVED | Handlers are SQL directly over the DB; there is no ORM impedance layer to bridge. |
| 75 | [#754](https://github.com/fastapi/fastapi/issues/754) | First-class session support | closed unresolved (highest real feature request) | **SHIPPED (oracle)** | The server-side session store IS a table (`quackapi_sessions`); `CREATE SESSION STORE` + `CREATE AUTH … AS SESSION` sugar, signed sid\|exp\|hmac cookies, synchronizer CSRF, lazy expiry, JWT-shaped claims so policies don't care which auth produced them. Merged `407200f`, tier-1 192/192. C mirror pending (`fleet/sessions-cpp`). Spec: [docs/specs/SESSION_CSRF_SPEC.md](docs/specs/SESSION_CSRF_SPEC.md). |
| 65 | [#617](https://github.com/fastapi/fastapi/issues/617) | Further develop startup/shutdown events | closed | **SHIPPED (oracle)** | Graceful SIGTERM drain shipped (R4); `CREATE LIFECYCLE ON STARTUP\|SHUTDOWN AS <sql>` merged `a7e14c5` (registry + `register_lifecycle`, tier-1 covered). C boot-runner (serve_brain executing STARTUP hooks at boot) pending. |
| 62 | [#1907](https://github.com/fastapi/fastapi/issues/1907) | Built-in readiness/liveness/startup probes | closed | **SHIPPED (oracle + C)** | `CREATE HEALTH CHECK` + `/livez` `/readyz` + `/metrics` as a SELECT over the log tables; merged `e520a0c`/`e2520f0`, C `HEALTH` sugar in `quackapi_extension.cpp`. |
| 57 | [#335](https://github.com/fastapi/fastapi/issues/335) | OAuth2 Authorization-Code flow tutorial/support | closed | PLANNED (rides outbound-POST verdict) | `CREATE AUTH ... AS OAUTH2` (authorize redirect + token exchange + JWKS verify). |
| 50 | [#1357](https://github.com/fastapi/fastapi/issues/1357) | Response model include/exclude fields | closed | **SHIPPED** | `CREATE ROUTE ... FIELDS(INCLUDE a, b \| EXCLUDE a, b)` projects the handler's JSON `body` — object or array (element-wise) — via `_apply_field_projection`, composed with the auth `_ctx` wrap as sibling CTEs. Oracle: tier-1 **156/156**. Compiled: boot+curl verified — object exclude/include, array exclude, **auth+projection composed** (policed route still 401s without a key), and the `CREATE ROUTE FIELDS` sugar path. See [framework.sql](framework.sql) (`_apply_field_projection`, `_compose_handler_sql`) and [ext-cpp/src/quackapi_brain.cpp](ext-cpp/src/quackapi_brain.cpp) (`compose_handler_sql`). |
| 49 | [#1773](https://github.com/fastapi/fastapi/issues/1773) | Automatically support HEAD for all GET routes | closed | SHIPPED (R4) | HEAD auto-answers every GET, zero-length body, Allow header computed. Parity 40/40; tier-1 112/112; live-verified (HEAD zero-body w/ correct CL, `Allow: GET, HEAD, POST`). See [docs/CHRONICLE.md](docs/CHRONICLE.md) (R4 section) and [framework.sql](framework.sql):533 (HEAD auto-included), :561 (HEAD matches GET routes). |
| 47 | [#1664](https://github.com/fastapi/fastapi/issues/1664) | "Very poor performance does not align with marketing" | closed | ANSWERED | Measured 42.6k req/s (health c8), 108k keep-alive (health c64 -k); 0 failures. See [bench/BENCH_HEADTOHEAD.md](bench/BENCH_HEADTOHEAD.md) and [docs/CHRONICLE.md](docs/CHRONICLE.md) (keep-alive ship + starvation gate: c64 -k `/health` 108,111 req/s). |
| 39 | [#270](https://github.com/fastapi/fastapi/issues/270) | Class-based views as first-class | closed | DEFERRED | Routing sugar; low leverage (note honestly). |
| 37 | [#1428](https://github.com/fastapi/fastapi/issues/1428) | Keycloak/OIDC integration | closed | PLANNED (rides #335) | Same OAuth2 machinery + an OIDC discovery URL. |
| 35 | [#10177](https://github.com/fastapi/fastapi/issues/10177) | HTTPBearer returns 403 instead of 401 | closed | CORRECT | Returns 401 for missing/invalid bearer. See [ext-cpp/A1_AUTH_RESULT.md](ext-cpp/A1_AUTH_RESULT.md) (11-case matrix, verified 2026-07-06): no-token/garbage/badsig/expired/injection → 401 "Unauthorized"; only valid+deny policy case → 403 "Forbidden". |
| 26-35 | [#11143](https://github.com/fastapi/fastapi/issues/11143) / [#10719](https://github.com/fastapi/fastapi/issues/10719) / [#1474](https://github.com/fastapi/fastapi/issues/1474) | Depends context-managers / Annotated deps / BaseModel-as-dependency | closed | **SHIPPED (oracle + C)** — narrow residual | `CREATE DEPENDENCY … SETUP … TEARDOWN …` shipped (merged `962c1af`/`1435a6f`): `dependencies`/`route_dependencies` + `run_dependency_phase`; C worker runs `setup; handler; teardown (always)` on `exec_con`; `test/di.test.sql`. **Residual honest edge:** a dependency holding a *live object across statements* (open cursor / streaming client) — teardown always runs, but object identity isn't threaded through like a Python generator frame. Rarely needed. See [docs/STATUS.md](docs/STATUS.md) "Honest edges". |

## Honest edges

quackapi does not (yet) solve everything. The genuinely open edges (verified 2026-07-09): **direct in-process TLS** is mechanism-limited (vendored mbedtls is crypto-only) so proxy termination is v1 — uvicorn's own answer; **inbound browser WebSockets** on the main port need C-server work (radio is client-only, outbound); **async I/O fan-out inside one handler** is conceded as different-not-equal; **multipart binary** v2 is an open call. Request-scoped DI setup/teardown is **shipped** (#11143) — the only residual is a dependency holding a *live object across statements*, rarely needed. gzip is **shipped** (in main, certified). The scoreboard above is only about the historic FastAPI "most wanted, closed unresolved" list — for the full current-state audit and the roadmap of what people want next, see [docs/STATUS.md](docs/STATUS.md).

Mirror to Notion (personal projects DB) per docs rule.
